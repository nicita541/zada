# Zada Mobile

The mobile package is a Capacitor shell for the shared web app. Android is the first target; iOS can be added later without changing the shared application layer.

```powershell
npm run build --workspace @zada/web
npm run sync --workspace @zada/mobile
npm run open:android --workspace @zada/mobile
```
