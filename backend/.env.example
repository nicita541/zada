PORT=3000
DATABASE_URL=postgres://taskbook:taskbook_password@db:5432/taskbook
CORS_ORIGIN=http://localhost:8080,http://127.0.0.1:8080
DEFAULT_USER_ID=local-user
PUBLIC_BASE_URL=http://localhost:8080
TZ=Asia/Novosibirsk

# Generate real keys before publishing:
# docker compose run --rm api node -e "import webpush from 'web-push'; console.log(webpush.generateVAPIDKeys())"
VAPID_PUBLIC_KEY=BNgK2rP6nM3v6Odmh2Fq0Vv-demo-local-key-replace-before-production
VAPID_PRIVATE_KEY=replace-before-production
VAPID_SUBJECT=mailto:admin@example.com
